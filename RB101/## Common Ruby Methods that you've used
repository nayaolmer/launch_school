## Common Ruby Methods that you've used

Array Mutation
(1) a.push(x) -- adds a value to the end of the array
(2) a << x -- adds a value to the end of the array
(3) a.unshift(x) -- as a value to the beginning of the array
(4) a.pop -- removed the last element of an array
(5) a.uniq -- return the unique values of an array
(6) a.delete(x) -- delete all instances of x in the array
(7) a.delete_at(x) -- delete the xth element of the array
(8) a.include?(x) -- check if the array contains x
(9) a.flatten -- mutate an array of arrays into a single, flattened array
(10) a.index(x) -- returns the first index value containing x



Random
[*1..n] << produces an array from the range 1..n


a.map { |n| n*2} -- returns an array
a.each {|n| puts n} -- returns each value
a.any?
a.all?
a.each_with_index {|n, index|}
a.each_with_object([]) {|n, object|}
a.first
a.partition

## Hashes

(1) merge two hashes together -- hash1.merge(hash2)